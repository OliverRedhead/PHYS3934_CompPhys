To sync manually: ``git sync-up``

Should sync automatically every 6 hrs

When I do work I should use the coursework branch ``git checkout coursework`` and push with ``git push -u coursework``. To merge, use 
```
git checkout coursework
git merge main
```
Hopefully this works =).