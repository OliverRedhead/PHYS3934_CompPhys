To sync manually: ``git sync-up``

(Should sync automatically every 6 hrs.)

I can check which branch I am on with ``git branch``
I can change between branches with ``git checkout BRANCH``. When I do work I should use the coursework branch ``git checkout coursework`` and push with ``git push -u coursework``
